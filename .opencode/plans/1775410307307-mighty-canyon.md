# Plan: app.py auf OpenRouter umstellen

## Ziel
Die app.py soll statt OpenAI den OpenRouter API nutzen. Der API Key ist bereits als `OPENROUTER_API_KEY` envvar gesetzt.

## Änderungen in app.py

### 1. Imports anpassen
- `ChatOpenAI` bleibt, da wir es mit `base_url` umkonfigurieren
- `OpenAIEmbeddings` bleibt, ebenfalls mit `base_url`

### 2. API Key aus EnvVar lesen
- Statt User-Eingabe: `os.environ.get("OPENROUTER_API_KEY")`

### 3. LLM Konfiguration ändern (Zeile 67)
```python
llm = ChatOpenAI(
    model="gpt-4o-mini", 
    openai_api_key=api_key,
    base_url="https://openrouter.ai/v1",
    temperature=0
)
```

### 4. Embeddings Konfiguration ändern (Zeile 30)
```python
embeddings = OpenAIEmbeddings(
    openai_api_key=_api_key,
    base_url="https://openrouter.ai/v1"
)
```

### 5. Sidebar Input entfernen (Zeilen 17-22)
- API Key Input Feld entfernen
- Stattdessen Prüfung ob EnvVar gesetzt ist

## Verifizierung
- App mit `streamlit run app.py` starten
- Prüfen ob Anfragen an OpenRouter gehen (keine Fehler)
